node_modules/
dist/
*.tsbuildinfo
.DS_Store
coverage/
*.png
!docs/*.png
!docs/assets/*.png
!docs/assets/**/*.png
!web/public/*.png
!site/assets/*.png
.playwright-mcp/

# Auto Claude data directory
.auto-claude/

# Auto Claude generated files
.auto-claude-security.json
.auto-claude-status
.claude_settings.json
.worktrees/
.security-key
logs/security/

# Test artifacts
test-results/

# Build symlink (created by test-server for Playwright)
src/web/static

# Task planning artifacts
.tasks/
.plan

# Claude local data. Existing public skills are tracked; explicitly allowlist
# new skills so local/private skills cannot be swept into this public repo.
.agents/
!.claude/
.claude/*
!.claude/skills/
.claude/skills/*

# Generated snapshots
todo-snapshot.md

# Local browser inspection captures (may contain session or user data)
original-deeplink-*
selector-open.yml
task-partial-*
.codex/hooks.json

# Playwright report artifacts
playwright-report/

# Pre-compiled daemon binaries (built by scripts/build-daemon.sh)
dist/daemon-binaries/

# Local-only Claude instructions (personal, never committed)
CLAUDE.local.md

# V8 isolate logs (contain local paths)
isolate-*.log

# Downloaded installer packages
*.pkg

# External plugin bundle cache (written by integration-loader.ts at runtime)
.plugin-cache/

# Local scratch / temp scripts and dumps
test-*-temp.*
test-*-tmp.*
sessions-list-*.md
playwright-responses/
tests/MEMORY_V2_*_PLAN.md
# repo-root scratch dir ONLY — a bare `tools/` would swallow src/agent/tools/ additions
/tools/

# Local demo assets (4K video, clips, thumbnails, launch kit) — large + local-only, never committed
demo/

# Local-only maintainer scripts (sensitive-content scanners — contain private grep patterns, never commit)
scripts/local/

# personal one-off migration scripts (contain private terms)
scripts/migrate-projects-to-skills.mjs
scripts/verify-and-purge-migrated.mjs

# ios-native (SwiftUI) — commit project.yml + sources; the .xcodeproj is xcodegen output
ios-native/Walnut.xcodeproj/
ios-native/DerivedData/
ios-native/build/
