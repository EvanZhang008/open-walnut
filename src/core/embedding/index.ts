/** Embedding subsystem barrel — only the config type remains; the engine lives
 *  in src/lib/hybrid-search/ and is wired up by src/core/search/. */

export type { EmbeddingConfig } from './types.js';
