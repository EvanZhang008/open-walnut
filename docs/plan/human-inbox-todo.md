# Human Inbox P1: implementation todo

Working tracker for docs/plan/human-inbox.md P1. Every agent checks off ITS OWN items as it completes them. Contract section below is FROZEN: parallel agents build against it; changing it requires updating this file first.

## Frozen contract (all agents build against this)

### Letter record (store)

```
{
  id: string,                      // 'lt-' + timestamp36 + '-' + rand
  subject: string,
  type: 'completion' | 'action_required' | 'review' | 'info',
  bodyFormat: 'html' | 'markdown',
  textPreview: string,             // <= 300 chars, plain text, for envelope + push
  sender: { sessionId, sessionTitle?, taskId?, taskTitle?, project?, host },   // stamped server-side
  createdAt: number, read: boolean, pinned: boolean, archived: boolean,
  actions?: Array<{ id: string, label: string, description?: string }>,
  answered?: { actionId: string, label: string, freeText?: string, at: number },
  thread: Array<{ from: 'agent' | 'human', text: string, bodyFormat?: 'html'|'markdown', bodyFile?: string, at: number }>
}
```

Storage: `WALNUT_HOME/human-inbox/index.json` (records, no bodies) + `WALNUT_HOME/human-inbox/bodies/<id>.html|.md` (+ `<id>.r<n>.<ext>` for rich thread replies). Body cap 200KB. Cross-process safety via updateJsonFile, same pattern as notifications store.

### Routes (new file src/web/routes/human-inbox-v1.ts, mounted from api-v1.ts, NOT server.ts)

- `POST /api/v1/human-inbox` send: `{subject, type, html?|markdown? (exactly one), text?, actions?, task_refs?, pin?}`; caller sid from `x-walnut-caller-sid` header; returns `{id}`
- `GET /api/v1/human-inbox?archived=1` list envelopes (index records, no body content)
- `GET /api/v1/human-inbox/:id` full letter: record + body content + thread body contents
- `POST /api/v1/human-inbox/:id/reply` agent thread reply `{html?|markdown?, text}`: flips read=false
- `POST /api/v1/human-inbox/:id/read` `{read: boolean}`
- `POST /api/v1/human-inbox/:id/pin` `{pinned: boolean}`
- `POST /api/v1/human-inbox/:id/archive` `{archived: boolean}`
- `POST /api/v1/human-inbox/:id/answer` `{actionId, freeText?}` human clicks an action: records `answered`, appends human thread entry, delivers to origin session
- `POST /api/v1/human-inbox/:id/human-reply` `{text}` human free-text reply: thread + deliver to origin session

### Ops (new src/ops/human-inbox.ts, imported in src/ops/index.ts)

- `human_inbox_send` bind POST /human-inbox; input mirrors route body; tags `{readonly:false, remote:'allow'}`
- `human_inbox_reply` bind POST /human-inbox/:letter/reply with `letter` path arg; same tags

### Event (src/core/event-types.ts)

- `'human-inbox:letter'` payload `{letterId, subject, type, textPreview, senderSessionId, senderTitle, host, kind: 'new'|'reply'}` — emitted by the store on send and on agent reply; consumed by the notification bridge, push, WS.

### Notification envelope

- New kind `'letter'` in NotificationKind; dedupKey `letter:<id>`; title=subject, body=textPreview, sessionId=origin sid; new optional field `letterId`; severity 'info'.
- Letters are EXEMPT from mark-all-read (markRead with no ids skips kind 'letter') and exempt from panel-open auto-read. Read state canonical in the LETTER store; the notification's own read flag mirrors it via the bridge.

### Delivery to origin session (answer / human-reply)

- `sendMessageToSession(originSid, wrapped, {source: 'human-inbox'})` through the existing queue; wrapper text carries the letter subject + the human's choice/text + one instruction: reply with `wn tools call human_inbox_reply '{"letter":"<id>", "text":"..."}'`. Dead session revives via the normal resume path; do NOT touch injectMidTurn/processNext internals.

### Files agents MUST NOT touch (other agents' uncommitted work)

package.json, src/web/server.ts, src/web/routes/sessions.ts, src/core/keep-awake.ts, src/core/phase.ts, src/core/diff-summary.ts, web/src/api/session-changes.ts, web/src/components/sessions/SessionDiffView.tsx, web/src/components/settings/sections/AdvancedSection.tsx, tests/core/keep-awake.test.ts, tests/core/phase-triage-gate.test.ts, tests/e2e/browser/keep-awake-settings.spec.ts, tests/e2e/permission-phase-flip.test.ts, probe-*.mjs, err_plain.txt, node_trace.1.log.

## A. Core: types + store + events (agent A)

- [x] A1. `src/core/human-inbox/types.ts`: LetterRecord, LetterType, LetterAction, ThreadEntry, NewLetter input type per contract
- [x] A2. `src/core/human-inbox/store.ts`: read/list (with archived filter), sendLetter (id gen, body file write, 200KB cap enforced, textPreview derivation from text||body, index insert), getLetter (record + body + thread bodies), agentReply, setRead/setPinned/setArchived, answerLetter (records answered, appends human thread entry), humanReply (appends thread entry); in-process write lock + updateJsonFile, mirroring notifications/store.ts
- [x] A3. sendLetter validates: subject non-empty, type in enum, exactly one of html|markdown, actions only when type=action_required, each action has id+label
- [x] A4. `'human-inbox:letter'` event added to src/core/event-types.ts; store emits on send and on agentReply
- [x] A5. store never throws on corrupt index (normalize like notifications store); body file missing → letter still lists, body renders an inline "body missing" note
- [x] A6. tests `tests/core/human-inbox-store.test.ts`: send/list/get roundtrip, validation failures, 200KB cap, read/pin/archive toggles, answer records + thread, reply flips read=false, corrupt index recovery, archived filter
- [x] A7. run `npm run test:focus tests/core/human-inbox-store.test.ts` green (26/26)

## B. Backend: caller plumbing + ops + routes + delivery (agent B)

- [x] B1. executor: `executeOp` opts gain `callerSid?`; when set, every request carries `x-walnut-caller-sid` header; gateway call site (capability-router.ts) passes its callerSid; local CLI/MCP paths read WALNUT_SESSION_ID from env when present
- [x] B2. `src/web/routes/human-inbox-v1.ts` implementing every route in the contract; body JSON limits consistent with letter cap; sender resolution: sid → session-tracker record → sessionTitle/taskId/taskTitle/project/host; unknown or absent sid → sender `{sessionId: 'external', host: 'local'}`
- [x] B3. mount router in src/web/routes/api-v1.ts (server.ts is forbidden)
- [x] B4. `src/ops/human-inbox.ts` with the two ops per contract + import in src/ops/index.ts; descriptions teach the writing standard in one line each (short, self-contained, actions for decisions)
- [x] B5. answer + human-reply handlers deliver to origin session per contract wrapper; delivery failure does NOT lose the thread entry (write first, deliver second, report delivery status in response)
- [x] B6. answering an already-answered letter → 409; unknown actionId → 400; answer/reply on archived letter → allowed (un-archives it)
- [x] B7. every route has a deadline and can never hang (repo rule); no sync blocking
- [x] B8. regenerate ops docs (`node scripts/generate-ops-docs.mjs`) so `npm run docs:check` passes with the two new ops
- [x] B9. tests `tests/web/human-inbox-routes.test.ts` via `startServer({port:0, dev:true})`: send→list→get roundtrip with sender stamping from header, validation 400s, answer flow incl. 409, human-reply, delivery wrapper content asserted against a mocked queue, ops parity (registry bindings resolve)
- [x] B10. run `npm run test:focus tests/web/human-inbox-routes.test.ts` green (19/19)

## C. Notification bridge + push (agent C)

- [x] C1. NotificationKind gains `'letter'`; NotificationRecord gains optional `letterId`; store.ts markRead-all EXEMPTS kind 'letter' (explicit ids still work). Also added `updateLetterNotification(letterId, patch)` (the envelope mirror the bridge patches) and made the 200-cap evict ordinary records BEFORE letters, so an error storm cannot push the human's inbox rows out of the feed (no letters in the feed = byte-identical to the old tail-slice)
- [x] C2. bridge module `src/core/notifications/letter-bridge.ts`: subscribes to `'human-inbox:letter'`, upserts the envelope notification (dedupKey `letter:<id>`), reply events flip the envelope unread + refresh the preview (and re-create the envelope if it had been dismissed). INIT SEAM: the only init point for the other bridges is server.ts (forbidden), so `ensureLetterBridge()` (idempotent) is called by `emitLetterEvent()` in `src/core/human-inbox/store.ts` — the store is the only producer of letter events, so "the store is in use" is exactly when the bridge must exist, and it runs before the emit. Broadcast to the WS feed is a lazily-imported `broadcastEvent` (tests inject a spy), so nothing in core needs a web server
- [x] C3. letter read-state sync: `mirrorLetterReadState(letterId, read)` (exported by the bridge, never throws) is called fire-and-forget from the letter store's `setRead`, `answerLetter` and `humanReply`; letter store stays canonical, the envelope only mirrors
- [x] C4. push-notification.ts handles the new event: title = `New letter: <subject>` / `Reply: <subject>` (trimmed to 100), body = textPreview, data `{type:'human_inbox_letter', letterId, letterType, kind}`; rides the existing maybePush "no WS clients" gate
- [x] C5. condition-system fit: the envelope carries no recoveryKey and no expiry. Verified every sweep is kind-gated so none can reach a letter: recoverNotifications / expireErrorNotifications / expireKeylessErrorNotifications filter `kind === 'operation-error'`, expireOrphanedPermissionNotifications + resolvePermissionNotification are `perm:`-keyed, expireStaleErrorNotifications only walks error records. Pinned by a test that runs all three error sweeps against a letter
- [x] C6. tests `tests/core/human-inbox-notifications.test.ts` (19 tests): bridge creates envelope on event + broadcasts it, external sender leaves sessionId off, duplicate event = one record, reply flips unread + refreshes preview, reply re-creates a dismissed envelope, store-driven end-to-end (sendLetter/agentReply install the bridge themselves), read mirroring both directions, mark-all-read skips letters while explicit-id works, error sweeps ignore letters, 210-record storm cannot evict a letter, push payload shape (+ title trim, empty preview, WS-client gate)
- [x] C7. run `npm run test:focus tests/core/human-inbox-notifications.test.ts` green (19/19; `npx tsc --noEmit` clean apart from B's in-flight human-inbox-v1.ts)

## D. Web UI: rail section + reader + actions (agent D — all web/src changes)

- [x] D1. api client `web/src/api/human-inbox.ts`: list/get/read/pin/archive/answer/humanReply, typed per contract (G fixed the delivery shape: the routes send `delivery` as an OBJECT `{status,reason,…}`, the client read it as a string and reported every send as 'unknown'; added `deliveryText()` + `skipped` status)
- [x] D2. client model: `NotificationSection` gains `'inbox'`; `sectionOf` maps kind 'letter' → 'inbox'; sectionCounts counts unread letters for the inbox tab; UNANSWERED action_required letters ALSO counted into 'action' (Needs Action shows them; answering removes them from action, they stay in inbox) — pinned by 3 new cases in tests/web/notification-model.test.ts (G)
- [x] D3. (finished by G) NotificationPanel: Inbox rail tab with its own unread count; envelope rows (sender/session name, task title, host, subject, textPreview, relative time, type badge, pin marker, unread dot); rows sorted pinned-first then newest; row actions pin/archive/mark-unread; panel-open mark-all-read continues to NOT touch letters (server already exempts; client must not optimistically clear them either)
- [x] D4. (G) `web/src/components/inbox/LetterReader.tsx` + `LetterBody.tsx`: large portal overlay (PlanPopup-scale); html body via sandboxed iframe srcDoc, markdown via renderMarkdownWithRefs; opening marks THAT letter read. Sandbox is `allow-popups allow-popups-to-escape-sandbox` (still no scripts, no same-origin) + an injected `<base target="_blank">`: with a bare `sandbox=""` a link in the body is a DEAD click, and with no scripts allowed there is no way to intercept it — popups are the only capability that makes D5's "links open a new tab" true for HTML bodies
- [x] D5. (G) reader interactivity: markdown bodies + thread bodies go through renderMarkdownWithRefs (task/session pills + file links for free) with clicks delegated to `useEntityClickHandler` (file links open a FileViewer scoped to the sender's host); `taskRefs` render as task pills under the body; HTML-body links open a new tab via `<base target="_blank">`
- [x] D6. (G) action buttons at reader top for unanswered action_required: click → POST answer → answered state renders (which option, when); free-text field under buttons; answered letters show the record read-only; a 409 from another surface re-reads instead of leaving the buttons armed
- [x] D7. (G) thread rendered under body via `LetterThread.tsx` (agent/human turns, timestamps, rich turn bodies); composer at bottom posts human-reply (⌘/Ctrl+Enter); delivery status shown non-blocking, with `skipped` phrased honestly ("Saved — this letter has no origin session to answer")
- [x] D8. (G) live updates: `useHumanInbox` refreshes (350ms coalesced) on every letter lane — `notification:new` / `notification:updated` with kind 'letter', plus `human-inbox:letter`. Verified live: a letter POSTed while the panel was open took the rail from 1 row/no badge to 2 rows/badge 1 with no refresh and no click
- [x] D9. (G) obeys the overlay rules: reader portals to body with `onPointerDown`/`onClick` stopPropagation, is bounded at 78vw/88vh (never overflows), and Escape is layered — the panel opts out while the reader is open, the reader opts out while its FileViewer is open (all three listeners sit on `document`, so stopPropagation alone could not have fixed this). Structured `log` only, full letter ids, no console.log
- [x] D10. `cd web && npx vite build` green (1m26s); `npx tsc --noEmit -p web/tsconfig.json` clean for every file under web/src

## F. Skill + guidance (agent F)

- [x] F1. walnut skill (src/data/skills/walnut/SKILL.md): "When to send a letter" section: the four types with one-line triggers, the writing standard (one phone screen, background in 1-2 sentences, long artifacts as links, actions for decisions), the exact `wn tools call human_inbox_send` example, reply instruction shape
- [x] F2. keep the section SHORT (this rides every session's skill index budget); verify generated docs still pass `npm run docs:check` after B8 (coordinate: F runs check only, B owns the regen)

## G. Integration verify (agent G, after A-F)

- [x] G1. `npm run build` green (tsup, 9.7s); `cd web && npx vite build` green (twice: after the D work and after the InboxPane extraction). `npx tsc --noEmit` at the repo root reports ONE error, `src/lib/hybrid-search/writer.ts` (another agent's untracked search work) — nothing from human-inbox; web/src is clean
- [x] G2. `npm run test:baseline` (the quick tier + the verdict) reports 9 failing / 1 in the recorded baseline → 8 "new". NONE are human-inbox: re-running those 8 files in isolation left 6 failures, all traceable to OTHER agents' uncommitted work in the shared tree (`personal-ai-lane` wants the OLD "Only a human may set COMPLETE" prompt text vs the new 4-phase wording; `cross-platform-ratchet` flags `src/providers/daemon-source.ts:107`; `session-auto-archive` is the forbidden `routes/sessions.ts`; `cloud-setup/job` crash-resume) or to load flakes (chat-history / task-manager / git-maintenance PASSED in isolation, and task-queue failed a DIFFERENT case each run with `withWriteLock timeout` under machine load 50-310). Feature evidence instead: one focus run over the 5 suites this change touches — notification-model + notifications/store + human-inbox store/notifications/routes — is **189/189 green**
- [x] G3. integration fallout fixed: (1) `sectionCounts` gained two fields, so `tests/web/notification-model.test.ts`'s exact-shape assertion failed — updated + 3 new letter cases added; (2) the web client parsed `delivery` as a string while the routes send an object, so every answer/reply reported "unknown" — fixed with `deliveryText()`; (3) `web/src/styles/human-inbox.css` was imported by `LetterEnvelopeRow` but never written (a missing-module BUILD failure) — written; (4) Escape closed the panel underneath the reader — layered; (5) closing the panel left `openLetterId` set so the next open re-popped the letter — cleared
- [x] G4. manual smoke on an ephemeral server (never 3456; `OPEN_WALNUT_API_URL` pointed the CLI at it): `tools list` shows both ops; `human_inbox_send` with no sid → sender `external`, with `WALNUT_SESSION_ID` → sessionId + sessionTitle + host stamped; GET list/detail return bodies; answer → `delivery {status:'skipped', reason:'no_origin_session'}` + answered record + human thread turn + auto-read; 2nd answer 409; unknown actionId 400; human-reply to a real origin session → `delivery {status:'queued', messageId:'qm-…'}`; `human_inbox_reply` op flipped the letter unread and stored its rich body; pin/archive/read toggles 200 and the archived feed holds exactly the archived letter; `GET /api/notifications` shows both `letter:<id>` envelopes with the reply preview and mirrored read state. Plus a real-UI pass (screenshots in /tmp/human-inbox/): rail badge, envelope rows, reader with the sandboxed iframe, decision buttons → answered, thread + composer, live arrival with no refresh, Escape layering, archived view — zero console errors
- [x] G5. D1-D10 were unfinished when G started (only the api client, the client model and `LetterEnvelopeRow` existed, and the CSS they imported was missing) — G built D3-D10 and checked the D block off above with per-item notes. New files G added: `web/src/components/inbox/{LetterReader,LetterBody,LetterThread,InboxPane}.tsx`, `web/src/styles/human-inbox.css`. Note for H: the reader's iframe sandbox is `allow-popups allow-popups-to-escape-sandbox` (NOT `sandbox=""` — see D4), the row class is `.hib-row`, the reader is `.hib-reader`, action buttons `.hib-action-btn`, composer `.hib-composer-input` + `.hib-send-btn`, and the rail tab is a `.nfc-rail-btn` labelled "Inbox"

## H. Real-UI E2E (agent H, after G)

- [x] H1. `tests/e2e/browser/human-inbox.spec.ts` (3 tests, all green): letters are POSTed the way `wn` does it (`/api/v1/human-inbox` + the caller-sid header) and the origin session is a REAL quick-start init-only session, so the delivery receipt is the mock CLI's OWN echo of the wrapped text in `/api/sessions/:id/history` (asserted: `[Letter reply]`, subject, chosen label, the `human_inbox_reply` instruction, the letter id) — not a status the UI printed. Also pinned: bell badge, Needs Action LISTS the unanswered action_required letter and drops it once answered, Inbox rail badge, envelope chips (session/task/host/preview/type), sandboxed iframe (`sandbox` is exactly `allow-popups allow-popups-to-escape-sandbox` and an inline `<script>` in the body does NOT run), one-click answer + free-text note → answered record, human free-text reply in the thread, `human_inbox_reply` flipping the letter unread + growing the thread LIVE with no reload + rendering the rich markdown turn, pin sorts first, read-on-open + mark-unread, archive/unarchive round trip, and unread surviving panel open/close/reopen. **Product bug found and fixed:** the answer/human-reply routes returned the bare index RECORD while the reader replaces its loaded document with that response, so answering blanked the letter body (empty white iframe) and made rich thread turns read "no longer on disk" — `letter-ops.ts` now answers with the same body-inlined detail `GET /:id` returns (`withBodies`), plus a keep-what's-on-screen guard in `LetterReader.applyResult` for an older/relayed payload. Pinned by two assertions in the spec
- [x] H2. ran through the pw gate (`PW_WORKERS=1 npx playwright test tests/e2e/browser/human-inbox.spec.ts`) — **3 passed (44.2s)**, twice green after the fix; machine load was 28-42 on 14 cores and nothing starved. Screenshots: `/tmp/human-inbox/e2e-01-inbox-rail.png`, `e2e-02-reader-html-body.png`, `e2e-03-answered.png`, `e2e-04-thread-and-composer.png`, `e2e-05-agent-reply-threaded.png`, `e2e-06-pinned-first.png`, `e2e-07-archived-view.png`, `e2e-08-unread-survives-panel-open.png`. Note for the next runner: the fixture server imports `src/` through tsx, so a SERVER-side change needs the :3457 fixture reaped (`scripts/pw-cleanup.sh clean`) before it takes effect — the web half is a live vite dev server and needs nothing
- [x] H3. real UI clicks only: the single `page.goto('/')` per test is the initial load, and every step after it is a click/fill on the real console (bell → rail tab → envelope row → action button → composer → row buttons → Archived toolbar → close). HTTP is used ONLY for the agent side of the feature (send / `human_inbox_reply` / quick-start seed) and for reading the origin session's history as the receipt. Serial + `PW LTR …<nonce>` subject scoping + floor-only count assertions, because the fixture server (and therefore the letter store) is shared

## I. Review + fix (agents I, J)

- [x] I1. code review of the full our-files diff (A-H): 9 findings reported to the orchestrator for J. Cleared as SAFE: path traversal on body files (LETTER_ID_RE + BODY_FILE_RE + `encodeURIComponent` on the op's `:letter` path arg, so `../` never reaches `path.join`), HTML XSS in the iframe (no `allow-scripts`/`allow-same-origin`; `target=_blank` is implicit-noopener so the escaped popup can't reverse-tabnab the console) and in the markdown path (DOMPurify), mark-all-read exemption (server `markRead` no-ids skips kind 'letter' AND client `markAllRead` skips it), concurrent index writers (every mutation is in-process lock + `updateJsonFile`, and the reply body-file name is assigned under the lock), the write-then-deliver order (a delivery failure keeps the thread entry and returns a STATUS), and the route deadline. Top findings: (a) HEAD is UNBUILDABLE — `session-controls.ts` shipped in commit 533c0589 imports the still-untracked `../human-inbox/relay.js`, so K1's commit MUST include all of `src/core/human-inbox/` + `src/core/notifications/letter-bridge.ts`; (b) delivering a human answer auto-denies the origin session's pending permission ask (the peers path refuses to send in that state for exactly this reason); (c) `buildLetterDeliveryText` interpolates unbounded, unfenced AGENT-authored subject/action-label into a message that says "This is your user speaking", aimable at another session via the spoofable caller-sid header — `buildPeerWrapper` fences + disclaims, this doesn't
- [x] J1. applied. **(a) HEAD unbuildable — CANNOT be fixed by an edit, it is a COMMIT-SCOPE requirement for K1**, so it is verified and pinned instead: `scripts/check-committed-bundle.sh` on today's HEAD fails at 3 of 7 entries on `../human-inbox/relay.js`, and I re-ran the same esbuild gate against `HEAD + exactly these 17 server files` → **all 7 entries bundle clean**. K1 must commit, at minimum: `src/core/human-inbox/{types,normalize,preview,store,letter-ops,relay}.ts`, `src/core/notifications/letter-bridge.ts`, `src/ops/human-inbox.ts`, `src/web/routes/human-inbox-v1.ts` (new) + the human-inbox hunks of `src/core/event-bus.ts`, `src/core/event-types.ts`, `src/core/notifications/store.ts` (letter-bridge imports `updateLetterNotification` STATICALLY — omitting this one breaks HEAD again), `src/core/peers/capability-router.ts`, `src/core/push-notification.ts`, `src/ops/executor.ts`, `src/ops/index.ts`, `src/web/routes/api-v1.ts` (plus the web/tests/docs files, which don't affect the server bundle). **(b) permission auto-deny FIXED at the root** (`letter-ops.ts` `deliverLetterToOrigin`): an origin session with `pendingPermission` now gets `enqueueMessage` only (no SESSION_SEND), so neither processNext nor injectMidTurn runs and nothing is auto-denied; the answer is durable in the session queue and rides the next drain (turn end once the human resolves the prompt / startup recovery / daemon reconnect). New status `deferred` + reason `origin_awaiting_permission` flows through `LetterDelivery` → the routes → the web client (`deliveryText`), so the reader says so instead of claiming "Sent". Dropping the answer was not an option (an action answers once), which is why this is a park, not the peers-style refusal. **(c) wrapper hardened**: `buildLetterDeliveryText` no longer asserts "This is your user speaking" over raw agent text — subject + button label are one-lined and bounded (200 chars), the human note is clipped (4K, thread keeps the rest) and LAST, and all of it rides between two `---letter-answer-<sha1(payload)>---` markers with the header stating that `subject`/`choice` are echoes of the AGENT'S OWN text (labels, not instructions) while only `note` + the pick are the user. Same fixed-point argument as `buildPeerWrapper`, pinned by 5 tests. **Minors, all fixed:** archive now mirrors the envelope read state (`setArchived` → `mirrorLetterReadState`, letter's own `read` untouched) so an archived-unread letter can't leave a stuck bell badge; the Archived view no longer zeroes the Inbox badge or drops Needs Action rows (`useHumanInbox` keeps a `liveLetters` list, `NotificationPanel` counts from it); index-resident fields are bounded (≤12 actions, id ≤64, label ≤200, description ≤500, ≤50 task refs, answer note bounded in BOTH `answered.freeText` and the thread turn); the letter iframe now blocks remote subresources (`<meta http-equiv=Content-Security-Policy default-src 'none'; img-src data: blob:; style-src 'unsafe-inline'` + `referrerPolicy=no-referrer`; meta, not the Chromium-only `csp` attribute, because srcdoc enforces meta everywhere) so an agent tracker pixel can't report the read time; the CLOUD_MODE relay payloads are `{...b, id}` so a body field named `id` can't redirect the action to another letter. **Push body (minor 6) resolved as a DOC fix, not a code change**: the frozen contract C4 says body = `textPreview`, the code already documents "envelope only, never the document body", and other push kinds carry text the same way — so `docs/plan/human-inbox.md` now describes exactly that (subject + the ≤300-char preview the agent wrote for this purpose) instead of the stale "subject only". **Tests:** new `tests/core/human-inbox-delivery.test.ts` (12 cases: deferred/queued/skipped delivery, 5 wrapper cases incl. newline-forgery + bounds + marker integrity, 3 index-cap cases, archive mirroring). ONE focus run over the 4 affected suites (`human-inbox-delivery` + `human-inbox-store` + `human-inbox-notifications` + `human-inbox-routes`) = **75/76**; the one failure was MY assertion (the header names the marker, so it occurs 3×, not 2×) — corrected and re-verified with a tsx probe of the two touched cases rather than burning a second vitest run. Real-UI proof for the web half: `PW_WORKERS=1 npx playwright test tests/e2e/browser/human-inbox.spec.ts` → **3 passed (34.5s)** on a fresh fixture (so the server changes were live), zero page errors. `npx tsc --noEmit` clean at the repo root; web/src clean

## Commit (main session, after everything)

- [x] K1. committed f3ac873e via `git commit --only -- <30 explicit paths>` (index held other agents' staged work — desktop/, cli.ts, server.ts, instance-lock — all left intact); husky sensitive scan + staged-blob gate passed; `scripts/check-committed-bundle.sh` green on the new HEAD (the J1 closure requirement)
- [x] K2. status line added to human-inbox.md; memory updated
- [ ] K3. DEPLOY BLOCKED 2026-08-22: prod :3456 had an outage (deploy duel + tree boot hang) and now runs a HEAD build under launchd; do NOT run dev-prod until the boot-hang owner (search-v2/instance-lock staged changes) confirms a fix — commit only, deploy later

## L. P3: wn env-less fallback — any agent on a daemon host (agent L)

- [x] L1. `resolveWnEndpoint(env, probe, uid)` in `src/providers/wn-cli.ts` (pure, probe injected) — env socket wins, else `wellKnownGatewaySocketPath()` (new in gateway-core.ts: `WALNUT_DAEMON_DIR || /tmp/open-walnut` + the socket filename), absent WALNUT_SESSION_ID → sid `external`, and TWO distinct exit-6 errors: "no daemon on this host" (path named) vs "refusing <path>" when the well-known path is not an owner-only socket owned by this uid (`isTrustedGatewaySocket`: isSocket + uid + `mode & 0o077 === 0`) — /tmp is world-writable, so a planted socket must never receive a request; blank env values count as absent
- [x] L2. shared `resolveGatewayCallerSid()` + `EXTERNAL_CALLER_SID` in gateway-core.ts (so the rule is ONE function both twins mirror, not two hand-copies): 'external' passes through, every other untracked sid still gets `unknown_caller` locally; `daemon-standalone.ts` handleGatewayLine calls it and logs `gateway: external caller (no session env)` — verified in the live daemon log
- [x] L3. hub side: tools.list/tools.call needed no change (callerSid is provenance only; `resolveLetterSender('external')` already stamps the external sender); `peers.list` marks no self explicitly (`!external && …`) and `peers.send` names the sender "external terminal" / shortId `external` instead of borrowing a session title. Everything else deliberately UNCHANGED so the label unlocks nothing: same op catalog, local-only ops still refused, same per-sender throttle key, target_awaiting_permission / target_archived / queue_full still refuse — pinned by tests
- [x] L4. node twin synced (`daemon-source.ts`): its `runWnMinimal` mirrors the fallback with the SAME three-part trust check, and its `resolveCallerSid` mirror passes 'external' through + logs it. `bash scripts/build-daemon.sh` green (3 binaries + sidecars). Ratchet note: the twin regexes are pinned in the new test, so a future edit to one twin fails loudly
- [x] L5. new file `tests/unit/peers/wn-env-fallback.test.ts` (23 cases, `npm run test:focus` → **23/23 green**; the 867s wall time was the vitest gate queuing behind another agent at machine load 155, tests themselves 324ms): endpoint resolution (env wins and never probes, no-env fallback, WALNUT_DAEMON_DIR override + trailing slash, sid-only-missing, blank values, both error messages), the trust rule row by row (planted regular file / another user's socket / group- or world-writable / uid-less platform), `resolveGatewayCallerSid` (external passes, tracked + alias still resolve, `EXTERNAL`/`external ` still refused), twin-parity regexes for both daemons incl. "neither twin loosens 0600", and the hub cases (no self row, catalog reachable, local-only still refused, throttle on the external key, "external terminal" wrapper with the no-authorization fence intact, permission-parked/archived/unknown targets still refused, cloud replica still refuses)
- [x] L6. docs/plan/human-inbox.md updated: status line, the "reach story" gap paragraph rewritten as shipped (with the two invariants: 0600 IS the credential, `external` is provenance not authorization) and the P3 phasing row. **LIVE VERIFIED** on the Mac's own daemon from a shell with both env vars unset: `wn tools list` (exit 0), `wn peers list` (no `*` self row; `--json` → 4341 peers, 0 self), `wn tools call walnut_status` (exit 0), daemon log carries `gateway: external caller (no session env)`; refusal paths also live — absent socket → exit 6 naming the path, a world-writable socket planted at the well-known path → exit 6 "refusing … 0600 socket mode is the gateway credential". **Bug found while verifying and fixed in the same file:** `wn <cmd> --json | …` was TRUNCATED at exactly 64KB (the pipe buffer) because the daemon dispatch does `process.exit(await runWnCli(...))` and process.exit discards stdout still queued for a pipe — an agent piping `wn` got invalid JSON. `writeStdout()` now awaits the write callback (5s cap so a runtime that never calls back can't hang wn); re-verified live: the same command now yields a complete 1.77MB JSON that parses. NOT done deliberately: no letter was POSTed from an external caller into the PROD inbox (that would leave noise in the human's own inbox); the external sender stamping was already proven in G4

## M. P2: phone (iOS) inbox (agents M1=impl, M2=verify)

- [x] M1. models + API client: `ios-native/Walnut/API/ModelsInbox.swift` (Letter/LetterKind/LetterAction/LetterSender/LetterAnswer/LetterThreadEntry/LetterDelivery + the list/detail/action-result wrappers) and `ios-native/Walnut/API/WalnutAPIInbox.swift` (list/get/read/pin/archive/answer/human-reply, riding WalnutAPI's shared send/decode funnel). ONE `Letter` type decodes both the envelope and the detail, so a row can paint the reader instantly while the body loads. Decode is lenient past id/subject/type (a newer server must not empty the inbox); `read`/`pinned`/`archived` are `var` so the store can flip them optimistically. Sending a letter is deliberately absent — that is an agent action (`wn`)
- [x] M2. `ios-native/Walnut/Stores/InboxStore.swift`: `@Observable @MainActor`, follows NotesStore exactly (disk-cache first paint off-main, `isActive` guard on every completion, `LifecycleSuspendable`). Live and Archived are SEPARATE arrays and `unreadCount` is DERIVED from the live one, so browsing Archived can never zero the badge (the same bug G3 fixed on the web). Refresh on foreground (`resumeForForeground`), pull-to-refresh, and on push arrival (`refreshFromPush`) — no timer, since the v1 events feed carries tasks/sessions only and a letter is an async artifact. Optimistic read/pin/archive with exact-field rollback; archive moves the row between lists and reverts both on failure
- [x] M3. `ios-native/Walnut/Views/Inbox/InboxView.swift` + `LetterEnvelopeRow.swift`: rows carry unread dot, pin marker, subject (bold while unread), the stamped sender line (session · task), the agent's preview, a type badge (warning-coloured only for an unanswered decision), host chip and relative time — all off the index record, so the list costs zero body fetches. Entry point = a 5th TAB ("Inbox", `envelope`, placed after Chat) with `.badge(inbox.unreadCount)`, which is the app's own navigation shape; `MainTabView.Tab` gains `.inbox`. Archived shelf behind a toolbar toggle; swipe leading = pin, trailing = archive + read/unread
- [x] M4. `ios-native/Walnut/Views/Inbox/LetterReaderView.swift` + `LetterHTMLBody.swift` + `LetterThreadView.swift`: html bodies go through a WKWebView with `allowsContentJavaScript = false`, `loadHTMLString(_, baseURL: nil)`, a non-persistent data store, navigation lockdown (only the in-memory document renders; a real link tap opens in Safari, everything else is cancelled) and a `<meta>` CSP `default-src 'none'; img-src data: blob:; style-src 'unsafe-inline'` — so an agent tracker pixel can't report the read time, matching the web reader's iframe posture. Height comes from `scrollView.contentSize` KVO (no JS available to measure with) with a 1pt dead band so the resize can't loop. Markdown bodies render through the app's `MarkdownView`; bodies over 60K chars are clipped WITH a visible note rather than silently. Decision buttons at the top (note field rides with the choice), answered record, thread with rich turns, composer in a bottom safe-area inset; opening marks THAT letter read, and a 409 re-reads instead of leaving the buttons armed
- [x] M5. `ios-native/Walnut/Core/LetterDeepLink.swift`: a mailbox on the `VoiceQuickAction` pattern (delivery arms it, the surface consumes it when it next appears, TTL 10min, injectable clock) because a push lands when no SwiftUI view exists yet. Routing is pure + nonisolated and accepts all THREE envelope shapes the same `data` object arrives in (flat, under `data`, under Expo's `body` — including as a JSON string), and validates the id against the server's `lt-<ts36>-<rand>` shape, since a push is untrusted input that becomes a URL path. A TAP arms the reader (`UNUserNotificationCenter` delegate, installed from `QuickActionDelegate.didFinishLaunching` because a cold-launch tap is dropped otherwise); a SILENT/background delivery only bumps `arrivals` so the badge refreshes — opening the app minutes later must not yank the user into a letter. NOTE for M2/N: the app still has no APNs registration (server push is Expo-token based and no token is minted on device), so this path is dormant until that lands; the routing rule and mailbox are unit-pinned
- [x] M6. `xcodegen generate` regenerated the project and the `sources: [Walnut]` / `[WalnutTests]` globs picked up all 10 new files (verified: each of ModelsInbox/WalnutAPIInbox/InboxStore/LetterDeepLink/InboxView/LetterEnvelopeRow/LetterReaderView/LetterThreadView/LetterHTMLBody/InboxContractTests appears 4× in the generated pbxproj, which is the normal per-file reference count). Two xcodebuild runs against `platform=iOS Simulator,name=iPhone 16 Pro` (derivedData `/tmp/human-inbox-ios/dd`): `build` → **BUILD SUCCEEDED**, `build-for-testing` → **TEST BUILD SUCCEEDED** (so WalnutTests incl. InboxContractTests and WalnutUITests compile too). 0 errors; 3 warnings total in the whole app, NONE from any new or edited inbox file (`TimelineTextCells.swift` iOS-17 deprecation, `VoiceQuickAction.swift` actor isolation, an AppIntents metadata note — all pre-existing). Logs: `/tmp/human-inbox-ios/xcodebuild.log`, `/tmp/human-inbox-ios/xcodebuild-tests.log`. Also RAN M8's suite on the simulator (it had only been compiled, never executed): `xcodebuild -only-testing:WalnutTests/InboxContractTests test-without-building` → **Executed 26 tests, 0 failures**, `TEST EXECUTE SUCCEEDED` (`/tmp/human-inbox-ios/xcodebuild-inboxtests.log`)
- [x] M7. **FULLY GREEN on a real simulator**, not just build-green. New repeatable driver `ios-native/tests/inbox/inbox-e2e.sh` (shaped after `tests/voice/voice-quickaction-e2e.sh`): isolated server on **:3463** under `env -i` + throwaway `OPEN_WALNUT_HOME` (refuses :3456; also avoids :3457/:3459/:3461/:3519), 3 letters POSTed the way `wn` does it (`POST /api/v1/human-inbox` + `x-walnut-caller-sid`), app installed on the booted iPhone 16 Pro and pointed at that server through the existing DEBUG launch hooks (`defaults write … walnut.serverUrl` + `walnut.keychainFallback.walnut.deviceToken`), then Maestro drives REAL taps, serialized on the shared `/tmp/maestro-ios.lock`. Proven end to end: the 3 letters land as envelope rows with the stamped `External agent` sender + `Mac` host chip + type badges, pinned letter first, tab badge 3; opening the decision letter renders the html DOCUMENT through the WKWebView **and its inline `<script>` does not run** (the control assertion is that the body's own `<h2>` text IS visible, so "the script output is not visible" is meaningful rather than vacuous — this closes M1's open concern); one tap on a decision button with a note answers it; a free-text reply threads; the back-navigation returns to a list where THAT letter is read (no dot, `✓ Answered` chip) and the badge dropped to 2. Every state claim is re-asserted **server-side over HTTP**, not read off the UI: `answered.actionId=retry-backoff`, the note, `read=true`, 2 human thread turns, and `unreadCount` 3 → 2 (reading one letter marks exactly one). Screenshots: `/tmp/human-inbox-ios/01-inbox-list.png`, `02-reader-html-body.png`, `03-answered.png`, `04-thread-reply.png`, `05-inbox-after-read.png`. **Two things the runs taught, both encoded in the script:** (1) five separate `maestro test` invocations cost five XCUITest driver startups and the driver died mid-flow twice at machine load 41-66 (`Failed to connect to /127.0.0.1:7001`) — everything is now ONE flow, one driver session; (2) Maestro's `back` is a left-edge SWIPE and does not pop a NavigationStack whose child is a ScrollView (the reader just stayed on screen, see `/tmp/human-inbox-ios/diag-back-gesture-did-not-pop.png`), so the flow taps the real chevron, whose id is `BackButton` (found by dumping the live hierarchy with `maestro hierarchy`). Load context: green run at load 28, after two driver deaths at 41-66 — the flake is the driver under load, never the feature
- [x] M8. `ios-native/WalnutTests/InboxContractTests.swift` (26 cases, WalnutTests style: JSON in, pure helpers asserted, no network): full + minimal + unknown-type decode, answered/archived letters stop asking for a decision, list + action-result envelopes, thread roles and rich turns; preview whitespace collapse and the 300-char cap, sender/host fallbacks (`external` → "External agent", `__local__`/`local` → "Mac", blank title → session prefix), pinned-then-newest ordering, 60K body clipping; every delivery status's human text (`deferred`/`skipped` explicitly NOT problems); push routing across the three envelope shapes, refusal of foreign pushes and of a traversal-shaped id, tap-vs-background arming, TTL incl. a backwards clock; and the security floor pinned directly — `LetterHTMLBody.webViewConfiguration()` has JavaScript off + a non-persistent store, and the wrapper document carries the CSP with no http(s) reference of our own. NOT RUN as a suite (simulator run is M7's): `xcodebuild build-for-testing` compiled app + tests green, and `swiftc -typecheck` over the whole app module is clean with zero warnings from any new file

## N. Round-2 review + commit

- [x] N1. adversarial review of the P2+P3 diff done: 6 findings reported to the orchestrator for N2 (top three: the node daemon twin still truncates piped `wn` output at 64KB — L6's `writeStdout` fix was never mirrored and the twin-parity test does not pin it; a markdown-format letter body on iOS fetches remote `![](https://…)` images through `AttachmentImageView`, so the tracker-pixel leak the HTML path's CSP blocks is wide open on the other body format; and `wn` is still only on a Walnut-spawned session's PATH, so a hand-started terminal gets `wn: command not found` and the P3 "zero setup" claim is not met — `env -i … zsh -lic 'command -v wn'` finds nothing). Also: `external` bypasses the `self_send` refusal (a session can self-inject by blanking WALNUT_SESSION_ID) and shares ONE throttle bucket across every host, both contradicting "no capability a tracked session lacks / same per-sender throttle"; walnut-peer-sessions/SKILL.md still says wn only works inside managed sessions. Cleared as SAFE: the socket trust rule (symlink/hardlink/planted-socket cases all fail uid or mode), the iOS WKWebView posture (JS off, baseURL nil, CSP blocks every remote subresource, nav delegate cancels all but a real http(s) link tap), push-id validation matching LETTER_ID_RE, frozen-v1 additivity (no existing v1 route or iOS model changed shape; all 8 human-inbox routes have a CLOUD_MODE relay), and old-daemon degradation (clear exit 6, never a hang)
- [x] N2. applied. **(1) node twin pipe truncation FIXED** (`daemon-source.ts` `runWnMinimal`): output is buffered and flushed before exit (`exitWn`, 5s cap), every exit site now `return exitWn(...)` since the flush is async. A/B on the materialized twin, stdout through a real pipe: HEAD's twin returned **65536 bytes, invalid JSON**; the fixed twin returns the whole **262921** and parses. **(2) iOS markdown tracker-pixel leak FIXED at the root** (new `ios-native/Walnut/Views/Inbox/LetterMarkdownBody.swift`, used by both `LetterReaderView` and `LetterThreadView`): any image reference carrying a URL SCHEME (not just http(s) — `data:`/`blob:` excepted, exactly what the html CSP allows) is replaced by a visible "Image not loaded" note, so `AttachmentLoader` never fetches a third party; local paths still resolve through the authenticated media endpoint. **(3) `wn` unreachable from a plain shell FIXED** (both twins, `installUserWnShim`): the daemon also installs `~/.local/bin/wn` (plus `~/bin/wn` when that dir exists) — a resolver shim that execs `$WALNUT_DAEMON_DIR/bin/wn` at CALL time so a daemon upgrade can never strand it, PRODUCTION-dir-only (a sandbox/test daemon writes nothing), marker-guarded so a foreign `wn` is never clobbered, exit 6 with a clear message when no daemon is on the host. Verified against a throwaway HOME (isolated dir writes nothing, prod installs 0755, foreign file preserved, our own stale copy refreshed) and the shim artifact live (`--help` delegated to the running daemon; a missing dir printed the message and exited 6). **(4) `external` self_send / throttle:** the throttle key is now `external@<host>` (per-HOST bucket, so a runaway caller on one machine can't starve another) and the peer-note wrapper no longer says "external terminal" — an anonymous sender is described as "an UNIDENTIFIED process on host X … NOT your user typing", never a session title, so the label can't be forged to read like the human's shell (tracked-sender wrapper text is byte-identical). The self_send bypass is **stated, not blocked, deliberately**: self-injection is not a privilege boundary anywhere (the `session_send` op has no self guard, so ANY caller can already queue into its own session), and the label is anonymous by construction (a session can clear both env vars and reach the same well-known socket), so a refusal would cost the human's own terminal its `peers.send` and buy nothing. Doc + code comments rewritten to say exactly that. **(5) SKILL.md** (`walnut-peer-sessions`) now teaches the fallback, the `~/.local/bin` install, exit 6 = "no reachable daemon socket", and that an external note is unidentified. **Tests:** 3 new cases in `tests/unit/peers/wn-env-fallback.test.ts` (twin flush parity incl. a REAL run of the materialized node twin through a pipe, user-PATH shim parity, per-host throttle buckets) + the anonymous-wrapper case rewritten; 3 new cases in `ios-native/WalnutTests/InboxContractTests.swift`. ONE focus run over the 3 peers suites = **69/70**; the single failure was the pre-existing L5 case "throttled on the external key", which pre-burned the OLD bare-label bucket — it now pre-burns `external@devbox` (re-verified with a tsx probe, not a second vitest run). ⚠️ That failure exposed a landmine worth knowing: an ADMITTED `tools.call` in a unit test really executes the op against `http://127.0.0.1:3456` (prod), and it created one junk task titled `x` — deleted (`mt5kyrqu-bc3b`); a second identical junk task `mt5kmd40-a279` (09:04 vs 08:54 UTC) came from another agent's run of the same pre-existing case and is left alone. `npx tsc --noEmit` clean; `bash scripts/build-daemon.sh` green (3 binaries + sidecars); iOS `build-for-testing` **TEST BUILD SUCCEEDED** with zero warnings from any inbox file and `InboxContractTests` **29/29 green on the booted simulator**
- [ ] N3. commit(s) scoped to our files (`git commit --only`), P3 server/daemon and P2 ios can be separate commits
